# Daily Todo List - 原生应用打包指南

## 📦 概述

本指南介绍如何将 PWA 应用打包成原生应用：

- 🪟 **Windows 可执行文件** (.exe, .msi)
- 🤖 **Android APK** 文件
- 🍎 **iOS 应用** (.ipa)

---

## 🪟 Windows 应用打包

### 方法 1：使用 PWA Builder（推荐）⭐

**优点**：无需编程，免费，自动更新

#### 步骤：

1. **部署 PWA**
   - 将 `dist` 文件夹部署到任意静态托管服务
   - 获得 HTTPS 地址：`https://your-domain.com/`

2. **使用 PWA Builder**
   - 访问 https://www.pwabuilder.com
   - 输入你的 PWA 网址
   - 点击 **"Start"**
   - 选择 **"Windows"** 选项卡
   - 填写应用信息：
     - 应用名称：Daily Todo List
     - 包名：com.yourcompany.todolist
     - 版本号：1.0.0
   - 点击 **"Generate"**
   - 下载 `.msix` 或 `.msixbundle` 文件

3. **安装 MSIX 包**
   - 双击下载的 `.msix` 文件
   - 点击 **"安装"**
   - 应用出现在开始菜单

#### 优点：
- ✅ 无需代码修改
- ✅ 自动同步 PWA 更新
- ✅ 支持 Windows 10/11
- ✅ 可发布到 Microsoft Store

---

### 方法 2：使用 Electron（需要 Node.js）

**优点**：完全原生体验，可访问系统 API

#### 安装依赖：

```bash
# 安装 Electron 和打包工具
cd /path/to/your/project
npm install electron --save-dev
npm install electron-builder --save-dev
```

#### 创建 Electron 主文件 `electron/main.js`：

```javascript
const { app, BrowserWindow } = require('electron');
const path = require('path');

function createWindow() {
  const mainWindow = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      nodeIntegration: false,
      contextIsolation: true,
    },
    icon: path.join(__dirname, '../dist/icon-512.png'),
    titleBarStyle: 'default',
    autoHideMenuBar: true,
  });

  // 加载 PWA 文件
  mainWindow.loadFile(path.join(__dirname, '../dist/index.html'));

  // 开发工具
  if (process.env.NODE_ENV === 'development') {
    mainWindow.webContents.openDevTools();
  }
}

app.whenReady().then(createWindow);

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') {
    app.quit();
  }
});

app.on('activate', () => {
  if (BrowserWindow.getAllWindows().length === 0) {
    createWindow();
  }
});
```

#### 更新 `package.json`：

```json
{
  "name": "daily-todo-list",
  "version": "1.0.0",
  "description": "简洁的每日任务清单管理工具",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "build": "npm run build:pwa && npm run build:electron",
    "build:pwa": "vite build",
    "build:electron": "electron-builder",
    "dist": "electron-builder --publish=never"
  },
  "build": {
    "appId": "com.yourcompany.todolist",
    "productName": "Daily Todo List",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64", "ia32"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ],
      "icon": "dist/icon-512.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Daily Todo List"
    }
  }
}
```

#### 构建 Windows 安装包：

```bash
# 构建 PWA
npm run build:pwa

# 构建 Electron 应用
npm run build:electron

# 或一键打包
npm run dist
```

#### 输出文件：

- `release/Daily Todo List Setup 1.0.0.exe` - 安装程序
- `release/Daily Todo List 1.0.0.exe` - 便携版
- `release/Daily Todo List 1.0.0.msi` - MSI 安装包

---

### 方法 3：使用 Tauri（Rust，体积更小）

**优点**：体积小，内存占用低，Rust 编写

#### 安装依赖：

```bash
cd /path/to/your/project

# 安装 Tauri CLI
npm install --save-dev @tauri-apps/cli

# 初始化 Tauri
npx tauri init
```

#### 配置 `tauri.conf.json`：

```json
{
  "build": {
    "beforeBuildCommand": "npm run build",
    "beforeDevCommand": "npm run dev",
    "devPath": "http://localhost:5173",
    "distDir": "../dist"
  },
  "package": {
    "productName": "Daily Todo List",
    "version": "1.0.0"
  },
  "tauri": {
    "allowlist": {
      "all": false
    },
    "bundle": {
      "active": true,
      "targets": "all",
      "identifier": "com.yourcompany.todolist",
      "icon": [
        "icons/32x32.png",
        "icons/128x128.png",
        "icons/128x128@2x.png",
        "icons/icon.icns",
        "icons/icon.ico"
      ]
    },
    "windows": [
      {
        "certificateThumbprint": null,
        "digestAlgorithm": "sha256",
        "timestampUrl": ""
      }
    ]
  }
}
```

#### 构建：

```bash
# 构建 Windows 应用
npm run tauri build

# 输出：
# src-tauri/target/release/bundle/msi/Daily Todo List_1.0.0_x64.msi
# src-tauri/target/release/bundle/nsis/Daily Todo List_1.0.0_x64-setup.exe
```

---

## 🤖 Android 应用打包

### 方法 1：使用 PWA Builder（推荐）⭐

**优点**：无需编程，免费，自动更新

#### 步骤：

1. **部署 PWA**（同上）
2. **访问 PWA Builder**
   - https://www.pwabuilder.com
   - 输入 PWA 网址
   - 选择 **"Android"** 选项卡

3. **填写信息**：
   - 包名：`com.yourcompany.todolist`
   - 应用名称：Daily Todo List
   - 版本号：1.0.0
   - 签名密钥：自动生成

4. **下载 APK**
   - 点击 **"Generate"**
   - 下载 `app-release.apk`

5. **安装 APK**
   ```bash
   # 通过 ADB 安装
   adb install app-release.apk
   
   # 或直接传输到手机安装
   ```

---

### 方法 2：使用 Bubblewrap（Google 官方工具）

**优点**：官方支持，灵活配置

#### 安装：

```bash
npm install -g @bubblewrap/cli
```

#### 初始化项目：

```bash
bubblewrap init --manifest=https://your-domain.com/manifest.json
```

#### 配置：

```javascript
// 生成的 twa-manifest.json
{
  "packageId": "com.yourcompany.todolist",
  "host": "your-domain.com",
  "name": "Daily Todo List",
  "launcherName": "TodoList",
  "display": "standalone",
  "themeColor": "#3b82f6",
  "navigationColor": "#3b82f6",
  "navigationColorDark": "#1d4ed8",
  "navigationDividerColor": "#3b82f6",
  "navigationDividerColorDark": "#1d4ed8",
  "signingConfig": {
    "file": "path/to/keystore.keystore",
    "alias": "android"
  },
  "splashScreenFadeOutDuration": 300,
  "enableNotifications": true,
  "shortcuts": [],
  "playBillingEnabled": false
}
```

#### 构建 APK：

```bash
# 构建
bubblewrap build

# 输出：app-release.apk
```

---

### 方法 3：使用 Android Studio + TWA

**优点**：完全控制，可发布到 Play Store

#### 步骤：

1. **安装 Android Studio**
   - 下载：https://developer.android.com/studio

2. **创建新项目**
   - 选择 **"Empty Activity"**
   - 包名：`com.yourcompany.todolist`
   - 语言：Java/Kotlin

3. **添加 TWA 依赖**

在 `app/build.gradle`：

```gradle
dependencies {
    implementation 'com.google.androidbrowserhelper:androidbrowserhelper:2.5.0'
}
```

4. **创建 TWA Activity**

`app/src/main/java/.../TwaLauncherActivity.java`：

```java
package com.yourcompany.todolist;

import com.google.androidbrowserhelper.trusted.LauncherActivity;

public class TwaLauncherActivity extends LauncherActivity {
    // 使用默认配置
}
```

5. **更新 AndroidManifest.xml**

```xml
<application
    android:label="Daily Todo List"
    android:icon="@mipmap/ic_launcher">
    
    <activity android:name=".TwaLauncherActivity"
        android:label="Daily Todo List">
        <meta-data android:name="android.support.customtabs.trusted.DEFAULT_URL"
            android:value="https://your-domain.com/" />
        
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>
```

6. **构建 APK**

```bash
# 在 Android Studio 中
Build → Build Bundle(s) / APK(s) → Build APK(s)

# 或使用 Gradle
./gradlew assembleRelease
```

7. **签名 APK**

```bash
# 生成密钥
keytool -genkey -v -keystore my-release-key.keystore -alias alias_name -keyalg RSA -keysize 2048 -validity 10000

# 签名 APK
jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 -keystore my-release-key.keystore app-release-unsigned.apk alias_name

# 优化 APK
zipalign -v 4 app-release-unsigned.apk app-release.apk
```

---

## 🍎 iOS 应用打包

### 方法：使用 PWA Builder

1. **访问 PWA Builder**
   - https://www.pwabuilder.com
   - 输入 PWA 网址
   - 选择 **"iOS"** 选项卡

2. **下载 Xcode 项目**
   - 生成 Xcode 项目
   - 下载 `.zip` 文件

3. **在 macOS 上构建**

```bash
# 解压
unzip iOSProject.zip

# 用 Xcode 打开
cd iOSProject
open *.xcodeproj

# 在 Xcode 中
# 1. 选择设备或模拟器
# 2. Product → Archive
# 3. Distribute App
```

---

## 📦 发布到应用商店

### Google Play Store

1. **注册开发者账号**
   - 费用：$25 一次性
   - https://play.google.com/console

2. **准备材料**
   - APK/AAB 文件
   - 应用图标 (512x512)
   - 截图 (至少 2 张)
   - 应用描述
   - 隐私政策链接

3. **上传应用**
   - 登录 Play Console
   - 创建新应用
   - 上传 APK/AAB
   - 填写应用信息
   - 提交审核

---

### Apple App Store

1. **注册开发者账号**
   - 费用：$99/年
   - https://developer.apple.com

2. **准备材料**
   - 签名后的 .ipa 文件
   - 应用图标 (1024x1024)
   - 截图（各种尺寸）
   - 应用描述
   - 隐私政策

3. **上传应用**
   - 使用 Xcode 或 Application Loader
   - 登录 App Store Connect
   - 创建新应用
   - 上传 .ipa
   - 提交审核

---

### Microsoft Store

1. **注册开发者账号**
   - 费用：$19 个人 / $99 公司
   - https://developer.microsoft.com

2. **准备材料**
   - MSIX 包
   - 应用图标
   - 截图
   - 应用描述

3. **上传应用**
   - 登录 Partner Center
   - 创建新应用
   - 上传 MSIX
   - 提交审核

---

## 🔧 常见问题

### Q: 如何自动更新？

**A**: 
- **PWA Builder**：自动同步 PWA 更新
- **Electron**：使用 electron-updater
- **TWA**：每次打开自动加载最新版本

### Q: 如何访问设备功能？

**A**:
- **PWA**：使用 Web API（受限）
- **Electron**：可使用 Node.js 模块
- **TWA**：通过 WebView 接口

### Q: 包体积对比？

| 方案 | Windows | Android | iOS |
|------|---------|---------|-----|
| PWA Builder | ~1MB | ~2MB | ~5MB |
| Electron | ~100MB | - | - |
| Tauri | ~10MB | - | - |
| TWA | - | ~2MB | - |

---

## 📚 推荐方案

### Windows
1. **PWA Builder**（最简单）
2. **Tauri**（体积小）
3. **Electron**（功能强）

### Android
1. **PWA Builder**（最简单）
2. **Bubblewrap**（官方推荐）
3. **TWA + Android Studio**（专业）

### iOS
1. **PWA Builder + Xcode**（唯一选择）

---

**祝你打包顺利！** 🎉
